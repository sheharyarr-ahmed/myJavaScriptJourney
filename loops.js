"use strict";

for (let courses = 1; courses <= 5; courses++) {
  console.log(
    `i have done ${courses} courses as of now in order to become an full stack developer.`
  );
}

for (let evenNumber = 1; evenNumber <= 20; evenNumber++) {
  if (evenNumber % 2 === 0) {
    console.log(`${evenNumber}`);
  }
}

for (let n = 1; n <= 10; n++) {
  console.log(`5 x ${n} = ${n * 5}`);
}
